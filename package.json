{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "types": "turbo types",
    "clean": "turbo clean && rm -rf ./node_modules && rm -rf ./.next"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.3.0"
  },
  "packageManager": "npm@9.3.1"
}